# Social Copy

Date: 2026-03-12

Status:
- LinkedIn published via Late API on 2026-03-12
- LinkedIn URL: https://www.linkedin.com/feed/update/urn:li:share:7437926553713446912/
- X not published: no connected `twitter/x` account in the active Late profile

## X

Built Nerdy's live AI tutor with Late.dev skills, Next.js 15, FastAPI, Deepgram STT, Cartesia TTS, and a Simli avatar over LiveKit. Shipped fast-path latency: `speech_end->tts_first_audio` p50 `363.6 ms`, p95 `658.97 ms`. `speech_end->stt_final` p95 `112.23 ms`.

## LinkedIn

We pushed Nerdy's live AI tutor into a much cleaner shipped state.

Current stack on the shipped fast path:
- Late.dev skill workflow
- Next.js 15 + React 19 frontend
- FastAPI session server
- Deepgram streaming STT
- Cartesia TTS
- Simli avatar sessions over LiveKit

Current runtime benchmark numbers from the repo:
- `speech_end->tts_first_audio` p50 `363.6 ms`
- `speech_end->tts_first_audio` p95 `658.97 ms`
- `speech_end->stt_final` p95 `112.23 ms`

The useful part for us was not just getting a demo on screen. The useful part was keeping the tutor loop pluggable end to end so we can swap STT, LLM, TTS, and avatar providers without rewriting the session server.

The public-provider comparison lane is still slower. The shipped fast path is the one currently closing the latency gate.

Repo snapshot now shows the live tutor session UI directly in the README.
